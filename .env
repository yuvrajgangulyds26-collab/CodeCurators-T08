# -------------------------------------------------------
#  Wildfire Predictor — Environment Variables
#  Copy this file to .env and fill in your real values.
#  NEVER commit .env to git.
# -------------------------------------------------------

# Flask secret key — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=monkeydluffyisthegreatestpirateever

# Gmail credentials for OTP emails
GMAIL_USER=wildfirepredictor@gmail.com
GMAIL_APP_PASSWORD=dtjuzvzhsaqcfwey

# SQLite database path (default is fine for local dev)
DB_PATH=users.db

# Set to "true" only for local development, never in production
FLASK_DEBUG=false
